var config={
    type: Phaser.AUTO,
    width:850,
    height: 650,
    scene:{
	preload: preload,
	create: create,
	update: update
    }

 };
var game = new Phaser.Game(config);

function preload() {
   //
}

function create() {
    //
}

function update() {
    //
}
